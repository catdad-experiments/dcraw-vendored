language: node_js

jobs:
  include:
    - name: linux
      os: linux
      node_js: '10'
      script:
        - npm ci
        - npm run compile
        - npm test
    - name: darwin
      os: osx
      node_js: '10'
      script:
        - npm ci
        - npm run compile
        - npm test
